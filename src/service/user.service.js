export const registerUserService = async () => {
    try {
    } catch (error) {
        return { success: true, error }
    }
}

export const loginUserService = async () => {
    try {
    } catch (error) {
        return { success: true, error }
    }
}
